coverage:
  status:
    project:
      default:
        target: 90%
    patch:
      default:
        target: 90%
  ignore:
     - "tests/.*"
     - "examples/.*"
     - "src/metatrain/experimental/.*"

comment: false
