architecture:
  name: experimental.pet
  training:  
    INITIAL_LR: 1e-4
    EPOCH_NUM_ATOMIC: 250
    SCHEDULER_STEP_SIZE_ATOMIC: 500000000
    EPOCHS_WARMUP_ATOMIC: 250

training_set:
  systems:
    read_from: qm9_reduced_100.xyz
    length_unit: angstrom
  targets:
    energy:
      key: U0
      unit: eV

test_set: 0.5
validation_set: 0.1
