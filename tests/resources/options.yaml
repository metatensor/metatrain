base_precision: 32

architecture:
  name: soap_bpnn
  training:
    device: "cuda"
    batch_size: 16
    num_epochs: 100

# Section defining the parameters for structure and target data
training_set:
  structures:
    read_from: "ethanol_reduced_100.xyz"
  targets:
    energy:
      key: "energy"

test_set: 0.1
validation_set: 0.1
